# Environment Variables - Production
# Set these in Heroku Config Vars or your deployment platform
# NEVER store production secrets in files!

# ⚠️ CRITICAL: This file is for documentation only
# Set all variables via: heroku config:set KEY=value --app suoops-backend

# Required Variables:
DATABASE_URL=<heroku-postgres-url>
REDIS_URL=<heroku-redis-url>
JWT_SECRET=<min-32-char-random-string>
PAYSTACK_SECRET=sk_live_<your-live-key>
PAYSTACK_PUBLIC_KEY=pk_live_<your-live-key>
BREVO_API_KEY=xkeysib-<your-api-key>
AWS_ACCESS_KEY_ID=<aws-key>
AWS_SECRET_ACCESS_KEY=<aws-secret>
AWS_S3_BUCKET_NAME=suoops-production
TWILIO_ACCOUNT_SID=<twilio-sid>
TWILIO_AUTH_TOKEN=<twilio-token>
FRONTEND_URL=https://suoops.com
ENVIRONMENT=production
SENTRY_DSN=<sentry-dsn>

# How to set in Heroku:
# heroku config:set JWT_SECRET=$(openssl rand -hex 32) --app suoops-backend
# heroku config:set PAYSTACK_SECRET=sk_live_xxx --app suoops-backend
# heroku config:set DATABASE_URL=<postgres-url> --app suoops-backend

# Verify settings:
# heroku config --app suoops-backend
